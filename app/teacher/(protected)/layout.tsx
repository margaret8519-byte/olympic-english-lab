import {redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import TeacherShell from"@/components/teacher/TeacherShell";
export default async function Layout({children}:{children:React.ReactNode}){const supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/teacher/login");const{data:profile}=await supabase.from("teacher_profiles").select("display_name").eq("id",user.id).maybeSingle();if(!profile)redirect("/teacher/login?error=not-teacher");return <TeacherShell name={profile.display_name}>{children}</TeacherShell>}
