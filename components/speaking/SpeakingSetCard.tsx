import {Clock3,LockKeyhole,MapPin,Mic2} from "lucide-react";import type {SpeakingSet} from "@/data/speaking/2022/types";
export default function SpeakingSetCard({set,onStart}:{set:SpeakingSet;onStart:(set:SpeakingSet)=>void}){const locked=set.needsFactFile||!set.factFile;return <article className={`speaking-set ${locked?"locked":""}`}><div className="set-icon">{locked?<LockKeyhole/>:<Mic2/>}</div><span className="set-status">{locked?"Требуется Fact File":"Готово к тренировке"}</span><h3>{set.title}</h3><p><MapPin/>{set.location}</p><div className="set-meta"><span><Clock3/>10 минут</span><span>3–4 мин. + Q&A</span></div>{locked?<div className="missing-file">Для этого варианта требуется дополнительный Fact File.<small>Материал пока недоступен</small></div>:<button className="speaking-primary" onClick={()=>onStart(set)}>Начать тренировку</button>}</article>}
