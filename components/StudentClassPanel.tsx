"use client";
import{FormEvent,useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";import type{JoinedClass}from"@/lib/supabase/student-profile";
export default function StudentClassPanel(){const[klass,setKlass]=useState<JoinedClass|null>(null),[open,setOpen]=useState(false),[error,setError]=useState("");useEffect(()=>{let active=true;const supabase=createClient();supabase.from("class_enrollments").select("classes(id,name,grade,class_letter)").limit(1).maybeSingle().then(({data})=>{const row=data?.classes as unknown as{id:string;name:string;grade:number|null;class_letter:string|null}|undefined;if(active&&row)setKlass({class_id:row.id,class_name:row.name,grade:row.grade,class_letter:row.class_letter})});const key="olympic-writing-submission-9-2024",marker=JSON.parse(localStorage.getItem(key)||"null");if(marker?.attemptId)supabase.from("writing_reviews").select("score,max_score,teacher_comment,status").eq("attempt_id",marker.attemptId).maybeSingle().then(({data})=>{if(data?.status==="reviewed"){localStorage.setItem(key,JSON.stringify({...marker,reviewStatus:"reviewed",score:data.score,maxScore:data.max_score,teacherComment:data.teacher_comment}));window.dispatchEvent(new Event("training-progress"))}});return()=>{active=false}},[]);async function join(event:FormEvent<HTMLFormElement>){event.preventDefault();const code=String(new FormData(event.currentTarget).get("code")||""),{data,error:joinError}=await createClient().rpc("join_class_by_code",{p_code:code});if(joinError){setError(joinError.message);return}const row=data?.[0] as JoinedClass|undefined;if(row){setKlass(row);localStorage.setItem("studentClass",JSON.stringify(row));const profile=JSON.parse(localStorage.getItem("studentProfile")||"{}");localStorage.setItem("studentProfile",JSON.stringify({...profile,grade:String(row.grade??profile.grade),classLetter:row.class_letter??profile.classLetter}));setOpen(false)}}if(klass)return <div className="student-class-label">Класс: {klass.grade}{klass.class_letter} — {klass.class_name}</div>;return <div className="student-class-join">{!open?<button onClick={()=>setOpen(true)}>Присоединиться к классу</button>:<form onSubmit={join}><input name="code" required placeholder="Код класса"/><button>Подключиться</button><button type="button" onClick={()=>setOpen(false)}>Отмена</button>{error&&<small>{error}</small>}</form>}</div>}
