"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
import {createClient} from "@/lib/supabase/client";
export default function TeacherAuthForm({mode}:{mode:"login"|"register"}){const router=useRouter(),[error,setError]=useState(""),[message,setMessage]=useState(""),[pending,setPending]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);setError("");const form=new FormData(event.currentTarget),email=String(form.get("email")||"").trim(),password=String(form.get("password")||""),displayName=String(form.get("displayName")||"").trim(),confirm=String(form.get("confirm")||"");if(mode==="register"&&password!==confirm){setError("Пароли не совпадают.");setPending(false);return}const supabase=createClient();await supabase.auth.signOut();if(mode==="register"){const{data,error:authError}=await supabase.auth.signUp({email,password,options:{data:{account_type:"teacher",display_name:displayName}}});if(authError){setError(authError.message)}else if(data.session){router.replace("/teacher");router.refresh()}else setMessage("Аккаунт создан. Подтвердите email, затем войдите.")}else{const{data,error:authError}=await supabase.auth.signInWithPassword({email,password});if(authError)setError(authError.message);else{const{data:profile}=await supabase.from("teacher_profiles").select("id").eq("id",data.user.id).maybeSingle();if(!profile){await supabase.auth.signOut();setError("У этого аккаунта нет профиля учителя.")}else{router.replace("/teacher");router.refresh()}}}setPending(false)}return <form className="teacher-auth-form" onSubmit={submit}>{mode==="register"&&<label>Имя учителя<input name="displayName" required minLength={2}/></label>}<label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Пароль<input name="password" type="password" required minLength={8} autoComplete={mode==="login"?"current-password":"new-password"}/></label>{mode==="register"&&<label>Подтвердите пароль<input name="confirm" type="password" required minLength={8} autoComplete="new-password"/></label>}{error&&<p className="teacher-error">{error}</p>}{message&&<p className="teacher-success">{message}</p>}<button className="primary blue" disabled={pending}>{pending?"Подождите…":mode==="login"?"Войти":"Зарегистрироваться"}</button><p>{mode==="login"?<>Нет аккаунта? <Link href="/teacher/register">Зарегистрироваться</Link></>:<>Уже есть аккаунт? <Link href="/teacher/login">Войти</Link></>}</p></form>}
